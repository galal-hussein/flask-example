machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t husseingalal/flask-example .

test:
  override:
    - docker images
    - docker run -it husseingalal/flask-example python manage.py test
