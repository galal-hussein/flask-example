machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t husseingalal/flask-example .

test:
  override:
    - docker run -itd hussienglala/flask-example python manage.py test
