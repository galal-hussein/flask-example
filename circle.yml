machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t husseingalal/flask-example .

test:
  override:
    - ls
    - docker images
    - docker run -v ./:/app -it husseingalal/flask-example python manage.py test
